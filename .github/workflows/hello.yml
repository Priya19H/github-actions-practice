# Workflow

name: Hello

# On tells when the workflow should run

on:
  workflow_dispatch:

# Job tells what the workflow should do

jobs:
# first job (gives indentation)
  say-hello:
  #Creates ephermael container with ubuntu running (after use delete automatically)
    runs-on: ubuntu-latest
    steps:
      - name: Say hello to everyone
      run: echo "Hello Dosto"



